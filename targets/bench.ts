import Benchmark = require('benchmark');
import SlackLike from '../markups/SlackLike';

const suite = new Benchmark.Suite;

const slackLike = new SlackLike();
const input = '*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` ``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ``` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote*bold* _italics_ ~strike~ `code` ```preformatted``` >quote';

// add tests
suite.add('format', function () {
  slackLike.format(input)
})
  // add listeners
  .on('cycle', function (event: any) {
    console.log(String(event.target));
  })
  .on('complete', function () {
    console.log('Fastest is ' + this.filter('fastest').map('name'));
  })
  // run async
  .run({'async': true});
