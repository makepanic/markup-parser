{
  "name": "markup-parser",
  "version": "0.1.0",
  "description": "A set of primitives for building string markup parsers",
  "main": "index.js",
  "scripts": {
    "pretest": "npm run build-test",
    "test": "nyc --reporter=html ava 'dist/tests/**/*.js'",
    "build": "tsc",
    "build-test": "tsc -p tsconfig.test.json",
    "build-gh-pages": "tsc -p tsconfig.gh-pages.json",
    "docs": "typedoc --out docs --mode file lib"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/makepanic/markup-parser.git"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "ava": "^0.21.0",
    "nyc": "^11.1.0",
    "open-color": "^1.5.1",
    "spectre.css": "^0.2.14",
    "surku": "^0.1.14",
    "typedoc": "^0.8.0",
    "typescript": "^2.4.2"
  },
  "dependencies": {}
}
